spring:
  application:
    name: hrms-backend

  datasource:
    url: jdbc:mysql://localhost:3306/hrms
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8081